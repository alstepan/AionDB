[package]
name = "aiondb-core"
version = "0.1.0"
edition = "2024"

[dependencies]
parking_lot = "0.12.5"
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
proptest = "1.10.0"
